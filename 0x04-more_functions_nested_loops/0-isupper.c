#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: character to be checked
 * Return: 1 if true, 0 if false
 */

int _isupper(int y)

{

	if (y >= 'A' && y <= 'Z')

		return (1);

	return (0);
}
